fixtures:
  forge_modules:
    stdlib:
      repo: "puppetlabs/stdlib"
    vcsrepo:
      repo: "puppetlabs/vcsrepo"
  repositories:
    wget: "https://github.com/rehanone/puppet-wget.git"
  symlinks:
    ohmyzsh: "#{source_dir}"
